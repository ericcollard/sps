{% extends 'base.html.twig' %}

{% block title %}Mise à jour de mot de passe{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-md-center">
        <div class="col-md-6">
        <h1>Mise à jour de mot de passe</h1>
        {{ form_start(requestPassForm) }}
        {{ form_row(requestPassForm.password) }}
        {{ form_end(requestPassForm) }}
        </div>
    </div>
</div>
{% endblock %}
